# 내부 호출 전용. gateway가 /api/deliveries → delivery-service:8084로 호출. order-service도 HTTP로 호출
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: metacoding
spec:
  type: ClusterIP
  selector:
    app: delivery
  ports:
    - port: 8084

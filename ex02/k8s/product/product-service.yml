# 내부 호출 전용. gateway가 /api/products → product-service:8082로 호출. order-service도 HTTP로 호출
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: metacoding
spec:
  type: ClusterIP
  selector:
    app: product
  ports:
    - port: 8082

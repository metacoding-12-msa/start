# 내부 호출 전용. order/product/delivery/orchestrator가 kafka-service:9092로 메시지 발행/구독
apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: metacoding
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - port: 9092

# 내부 호출 전용. 모든 서비스가 db-service:3306으로 DB 연결
apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: metacoding
spec:
  type: ClusterIP
  selector:
    app: db
  ports:
    - port: 3306

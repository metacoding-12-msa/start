apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-deploy
  namespace: metacoding
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product
  template:
    metadata:
      labels:
        app: product
    spec:
      containers:
        - name: product-server
          image: metacoding/product:3
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: product-configmap
            - secretRef:
                name: product-secret
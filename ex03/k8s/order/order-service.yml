# 내부 호출 전용. gateway가 /api/orders → order-service:8081로 호출. kafka-service로 이벤트 발행
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: metacoding
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
    - port: 8081
